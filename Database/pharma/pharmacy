create table pharmacy (
    id int primary key generated by default as identity,
    articul int not null,
    name varchar not null,
    price int not null,
    updated_time timestamp not null
);

create index pharmacy_updated_time_articul_name
    on pharmacy(updated_time, articul, name);

alter table pharmacy add column cat_id int references pharmacy_category(id) ON DELETE SET NULL;
